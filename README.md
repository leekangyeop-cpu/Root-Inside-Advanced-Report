# Root Inside - AI-Native Report Pipeline (V3.0: Schema & Security Enforced)

이 저장소는 루트인사이드(Root Inside) 경영 컨설팅 리포트의 1단계 공정(데이터 구조화 및 초안 작성)을 담당하는 로컬 파이프라인이자 중앙 데이터베이스(Semi-DB)입니다. 

본 시스템은 'VS Code Agent(데이터 파서)'와 'Claude Desktop(수석 컨설턴트)'을 완벽히 분리하여, **환각(Hallucination) 제로**의 무결점 리포트를 생산합니다.

---

## 🔒 [Security & Compliance: 보안 및 격리 규칙]
1. **Private Repository 강제:** 본 저장소(또는 파생 저장소)는 클라이언트의 민감한 재무/전략 정보가 포함되므로 반드시 **Private(비공개)** 상태를 유지해야 합니다.
2. **저장소 분리 권장:** 다수의 클라이언트 데이터가 섞이는 것을 방지하기 위해, 본 저장소는 'Standard Template'으로만 유지하고 실제 프로젝트는 클라이언트별로 Private Repository를 새로 생성(Clone)하여 작업하는 것을 원칙으로 합니다.

---

## 🛑 [Agent System Instruction: 에이전트 절대 준수 규칙]
**VS Code Agent는 작업을 시작하기 전, 자신의 역할을 '감정 없고 정확한 데이터 파서(Data Parser)'로 엄격히 제한하라.**

1. **창작 및 추론 절대 금지:** 제공된 `00_sources/` 원문에 없는 내용, 숫자, 의미를 스스로 지어내지 마라.
2. **선택적 추출 (No Noise):** 원문 전체를 100% 텍스트로 옮기는 것이 아니다. 원문 전체 보관은 `00_sources/`에서 충족하므로, 에이전트는 **'최종 리포트 결론에 사용될 핵심 데이터와 근거'**만을 `01_structured/`에 구조화하여 추출하라.
3. **스키마 강제 (Schema Strictness):** 아래 정의된 JSON/CSV 스키마를 단 하나의 필드라도 임의로 변경하거나 누락해서는 안 된다.

---

## 🏗️ 데이터베이스 스키마 (Semi-DB Schema)
모든 추출 데이터는 `01_structured/` 디렉토리에 아래의 정확한 규격으로 저장되어야 자동 검증 및 렌더링이 작동합니다.

- **`sources.json`**: `{ "id", "title", "type", "date", "path_or_url" }`
- **`claims.json`**: `{ "id", "text", "importance(high/med/low)", "requires_rebuttal(bool)" }`
- **`evidences.json`**: `{ "id", "source_id", "quote", "location(page/section)", "reliability(high/med/low)" }`
- **`metrics.json`**: `{ "id", "name", "value", "unit", "period", "source_id", "location" }`
- **`claim_evidence.csv`**: `claim_id, evidence_id` (주장과 근거의 1:N 매핑 테이블)

---

## 🛡️ 무결성 검증 규칙 (02_checks / QA Rules)
에이전트(또는 Python 스크립트)는 데이터를 추출한 후 반드시 아래 8가지 하드 룰을 검증하고, 결과를 `02_checks/validation_report.json`에 기록해야 합니다. 데이터 변경 시 반드시 이 검증을 다시 돌려야 합니다.

1. **Evidence 부족:** `importance`가 "high"인 Claim에 매핑된 Evidence가 2개 미만인가?
2. **고아 데이터:** `source_id`가 매핑되지 않은 Claim이나 Metric이 존재하는가?
3. **단위/기간 누락:** Metric 데이터에 `unit` 또는 `period` 값이 비어있는가?
4. **수치 모순:** 동일한 Metric 이름(예: 매출액)이 서로 다른 Value로 중복 존재하는가?
5. **기간 혼재:** 서로 다른 Period(예: 2024(연간) vs 2025_Q1(분기))가 명시 없이 섞여 있는가?
6. **최신성 경고:** `sources.json`의 Date가 현재 시점 기준 1년 이상 경과했는가?
7. **가정 누락:** 미래 추정치(Metric)가 있는데 관련된 가정이 없는가?
8. **리스크 부재:** 리포트 결론을 구성할 Claim 중 반박/리스크(`requires_rebuttal`)가 정의되지 않은 항목이 있는가?

---

## 🚀 Quick Start (인간의 실행 가이드)
새로운 컨설팅 리포트를 작성할 때 다음 순서를 따르십시오.

1. **프로젝트 셋업:** 클라이언트용 Private Repository를 생성하고 이 템플릿을 복사합니다.
2. **원문 투입:** 클라이언트의 재무제표, 인터뷰 록 등을 텍스트/CSV로 변환하여 `00_sources/`에 넣습니다.
3. **구조화 지시 (VS Code Agent):** > *"00_sources의 파일을 읽고, README의 스키마에 맞춰 01_structured 내부의 JSON 파일들을 채워줘."*
4. **자동 검증 (Terminal):** > `python ops/run_validate.py` (이때 8대 QA 룰이 작동하며 에러를 뱉어냅니다.)
5. **초안 생성 (Terminal):** 에러를 모두 수정했다면,
   > `python ops/run_render.py` (03_report/report_draft.md 가 생성됩니다.)
6. **최종 윤문 (Claude Desktop):** 생성된 `report_draft.md`를 Claude Desktop Project에 업로드하여 KPMG 스타일의 최종 리포트로 포맷팅합니다.
